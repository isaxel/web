Сделать “режим DNS-over-HTTP”: координаты передаются через TXT-запросы к поддомену, ответ - через DNS-запись. WildFly выступает как авторитативный DNS-серверСделать “режим DNS-over-HTTP”: координаты передаются через TXT-запросы к поддомену, ответ - через DNS-запись. WildFly выступает как авторитативный DNS-сервер