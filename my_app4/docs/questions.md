# 1/ Optimistic update
**Optimistic update (оптимистичное обновление)** в Java-веб-приложениях — это подход к работе с данными, при котором система **предполагает, что конфликты редки**, и не блокирует запись заранее. Вместо блокировок проверяется, **изменялись ли данные кем-то ещё** между чтением и сохранением.

Идея:

> «Я обновлю данные только если они всё ещё в том состоянии, в котором я их читал».

---

## 🧠 Зачем это нужно

В веб-приложениях много параллельных пользователей. Без контроля можно легко перезаписать чужие изменения:

1. Пользователь A открыл запись
2. Пользователь B открыл ту же запись
3. A сохранил изменения
4. B сохранил изменения → изменения A потеряны

Optimistic update предотвращает такую ситуацию.

---

## ✅ Как это работает

Обычно используется **версионирование записи**:

* В таблице есть поле `version` (или `updated_at`)
* При чтении клиент получает текущую версию
* При обновлении версия проверяется в `WHERE`

Если версия изменилась → значит был конфликт.

---

## 🔧 Пример на Java (JPA / Hibernate)

В JPA есть встроенная поддержка через `@Version`.

### Entity

```java
import jakarta.persistence.*;

@Entity
public class Product {

    @Id
    @GeneratedValue
    private Long id;

    private String name;
    private int quantity;

    @Version
    private Long version;

    // getters / setters
}
```

---

### Что делает `@Version`

Hibernate автоматически:

1. Читает `version`
2. При `UPDATE` добавляет проверку:

```sql
UPDATE product
SET name=?, quantity=?, version=version+1
WHERE id=? AND version=?
```

Если строка не обновилась → был конфликт.

---

### Обработка конфликта

```java
try {
    product.setQuantity(10);
    entityManager.merge(product);
} catch (OptimisticLockException e) {
    // кто-то изменил запись раньше
    // можно перечитать данные и показать сообщение пользователю
}
```

---

## 🚀 Поведение в веб-приложении

Типичный сценарий:

1. Клиент загружает данные → получает `version`
2. Пользователь редактирует форму
3. Сервер сохраняет → проверяет `version`
4. Если версия не совпала → вернуть ошибку / предложить перезагрузку

---


## 🆚 Optimistic vs Pessimistic

| Подход          | Механика                                        |
| --------------- | ----------------------------------------------- |
| **Optimistic**  | Не блокирует, проверяет конфликт при сохранении |
| **Pessimistic** | Блокирует запись заранее (`SELECT FOR UPDATE`)  |




# 2/ Mutual TLS
**Mutual TLS (mTLS)** — это расширение протокола TLS, при котором **обе стороны соединения подтверждают свою подлинность с помощью сертификатов**.

В обычном HTTPS:

* Клиент проверяет сертификат сервера ✅
* Сервер не проверяет сертификат клиента ❌

В **mTLS**:

* Клиент проверяет сервер ✅
* Сервер проверяет клиента ✅

---

## 🔐 Как выглядит процесс

Упрощённый TLS handshake:

1. Клиент подключается к серверу
2. Сервер отправляет свой сертификат
3. Клиент проверяет его
4. **Сервер запрашивает сертификат клиента** ← отличие от обычного TLS
5. Клиент отправляет сертификат
6. Сервер проверяет его
7. Соединение устанавливается

Если сертификат клиента недействителен → соединение отклоняется.

---

## 📜 Что такое сертификат клиента

Это X.509 сертификат, содержащий:

* публичный ключ клиента
* информацию о владельце
* подпись CA (Certificate Authority)

---

## 🔧 Пример в Java (Spring Boot / Tomcat)

mTLS включается на уровне TLS-конфигурации.

### application.yml

```yaml
server:
  ssl:
    enabled: true
    key-store: classpath:server-keystore.p12
    key-store-password: secret
    trust-store: classpath:server-truststore.p12
    trust-store-password: secret
    client-auth: need   # ← включает mTLS
```

---

### Что означают параметры

* **key-store** → сертификат сервера
* **trust-store** → кому сервер доверяет (CA клиентов)
* **client-auth: need** → клиент обязан предъявить сертификат


---

## 🆚 mTLS vs OAuth / JWT

| Механизм  | Идентификация             |
| --------- | ------------------------- |
| mTLS      | Сертификат + криптография |
| OAuth/JWT | Токен / подпись           |

Часто используются вместе:

✔ mTLS → подтверждает сервис
✔ JWT → передаёт user identity / claims

---




# 3/ Двухфазные и трехфазные конструкторы в Java EE
В Java EE (и вообще в Java) термины **«двухфазный» и «трёхфазный конструктор»** — это не официальные конструкции языка, а **архитектурные паттерны инициализации объектов**, которые применяются в контейнерных средах.

Важный контекст: в Java EE жизненный цикл объекта часто **контролируется контейнером**, поэтому конструктор — не единственное место инициализации.

---

# 🧩 Почему это вообще появилось

В обычной Java:

```java
MyService s = new MyService(); // конструктор делает всё
```

В Java EE:

* объект создаёт контейнер
* зависимости внедряются позже
* ресурсы доступны не сразу
* прокси / AOP / interceptors

Из-за этого **нельзя делать сложную логику в конструкторе**.

---

# ✅ Двухфазная инициализация (Two-phase construction)

Объект создаётся в **две стадии**:

### **Фаза 1 — Конструктор**

Минимальная инициализация. Никаких зависимостей, ресурсов, БД.

```java
public class PaymentService {

    public PaymentService() {
        // только простая логика
    }
}
```

---

### **Фаза 2 — PostConstruct**

Контейнер уже внедрил зависимости → можно работать.

```java
import jakarta.annotation.PostConstruct;
import jakarta.inject.Inject;

public class PaymentService {

    @Inject
    private PaymentRepository repo;

    public PaymentService() {
        // НЕЛЬЗЯ использовать repo здесь
    }

    @PostConstruct
    public void init() {
        // repo гарантированно доступен
    }
}
```

Контейнерный порядок:

1. Вызов конструктора
2. Dependency Injection
3. Вызов `@PostConstruct`

---

## 🧠 Зачем это нужно

В конструкторе:

❌ нет injected зависимостей
❌ нет транзакций
❌ нет JNDI ресурсов
❌ нет security context

В `@PostConstruct`:

✅ всё уже готово

---

# ✅ Трёхфазная инициализация (Three-phase construction)

Используется, когда жизненный цикл ещё сложнее.

### **Фаза 1 — Конструктор**

Самый базовый state.

---

### **Фаза 2 — Dependency Injection**

Контейнер внедряет зависимости.

---

### **Фаза 3 — PostConstruct / Lazy Init / Business Init**

Дополнительная инициализация, иногда отложенная.

Пример:

```java
@ApplicationScoped
public class CacheService {

    @Inject
    ConfigService config;

    private Cache cache;

    public CacheService() {
        // никаких ресурсов
    }

    @PostConstruct
    public void setup() {
        // можно читать config
    }

    public Cache getCache() {
        if (cache == null) {          // ← третья фаза (lazy)
            cache = buildCache();
        }
        return cache;
    }
}
```

---

## 🧠 Где встречается трёхфазная схема

✔ Lazy initialization
✔ Работа с удалёнными сервисами
✔ Дорогие ресурсы (pool, cache, connection)
✔ Когда нужен runtime context

---

# ⚠️ Почему нельзя делать всё в конструкторе в Java EE

Контейнер может:

* создать прокси вместо объекта
* не завершить injection
* управлять транзакциями отдельно
* переопределить lifecycle

Типичная ошибка:

```java
public MyBean() {
    repo.save(...); // 💥 NullPointerException
}
```

---

# ✅ Правильная практика в Java EE / Jakarta EE

В конструкторе:

✔ только простые поля
✔ никаких CDI / EJB / EntityManager
✔ никаких транзакций

В `@PostConstruct`:

✔ инициализация зависимостей
✔ регистрация listeners
✔ загрузка данных
✔ подключение к ресурсам

---

# 🆚 Разница между схемами

| Подход         | Идея                                                          |
| -------------- | ------------------------------------------------------------- |
| **Двухфазный** | Constructor → PostConstruct                                   |
| **Трёхфазный** | Constructor → Injection → PostConstruct / Lazy / Runtime Init |

---

# 🧠 Где это критично

Особенно важно для:

✔ CDI Beans
✔ EJB
✔ Managed Beans
✔ Spring (аналогичная модель)
✔ Любых DI-контейнеров

---


# Есть ли в Angular VDOM? Что используется вместо него? Incremental DOM
Короткий ответ: **в Angular нет классического VDOM**, как в React. Вместо него Angular использует **Incremental DOM-подобный подход** (исторически) и собственный механизм **change detection + прямые инструкции для обновления DOM**.

Разберёмся по-человечески 👇

---

# ❌ Есть ли в Angular Virtual DOM?

В Angular **нет Virtual DOM в привычном смысле**:

* нет отдельного дерева виртуальных узлов
* нет diff двух VDOM-деревьев
* нет reconciliation как в React

Angular **не пересоздаёт представление в памяти**, чтобы потом сравнивать.

---

# ✅ Что Angular делает вместо VDOM

Angular компилирует шаблоны в **инструкции обновления DOM**.

Идея:

> «Не создаём виртуальное дерево. Сразу знаем, что и когда менять».

Шаблон:

```html
<div>{{title}}</div>
```

После компиляции превращается примерно в:

```ts
ɵɵelementStart(0, "div");
ɵɵtext(1);
ɵɵelementEnd();
ɵɵtextBinding(1, ctx.title);
```

Angular просто обновляет **конкретный текстовый узел**, без диффинга деревьев.

---

# 🧠 Как это концептуально работает

Angular хранит:

✔ ссылки на реальные DOM-узлы
✔ биндинги
✔ структуру view

При change detection:

1. Проверяется, изменились ли значения
2. Если изменились → вызываются DOM-инструкции
3. Меняется только нужный узел

Без промежуточного VDOM.

---

# 📌 Где тут Incremental DOM?

Термин часто всплывает, потому что подход **очень похож на Incremental DOM**, который разработал Google.

**Incremental DOM идея:**

* нет виртуального дерева
* шаблон → инструкции
* обновляется только то, что реально изменилось

Angular не использует библиотеку Incremental DOM напрямую, но **архитектурно действует так же**.

---

# 🆚 VDOM vs Angular подход

| Механизм                | Как работает                                         |
| ----------------------- | ---------------------------------------------------- |
| **Virtual DOM (React)** | Строится новое виртуальное дерево → diff → patch DOM |
| **Angular**             | Change detection → прямые DOM-инструкции             |

---



# ⚙️ Что реально управляет обновлениями в Angular

Ключевые механизмы:

### 1. **Change Detection**

Отслеживает изменения в моделях.

Режимы:

* Default (грязная проверка)
* OnPush (реактивный, быстрее)

---

### 2. **Zone.js**

Перехватывает async операции и триггерит change detection.

---

### 3. **Ivy Renderer**

Современный runtime Angular:

* компилирует шаблоны в инструкции
* работает без VDOM
* минимизирует DOM-операции

---

# ✅ Итог

В Angular:

❌ нет Virtual DOM
✅ используется instruction-based rendering
✅ обновляется реальный DOM напрямую
✅ архитектура похожа на Incremental DOM

---


# Как работает JWT
JWT (**JSON Web Token**) — это компактный формат токена для **передачи утверждений (claims)** между сторонами, чаще всего для **аутентификации и авторизации**.

Главная идея:

> Сервер не хранит сессию. Вся информация уже внутри токена.

---

# 🧩 Структура JWT

JWT — это строка из **трёх частей**, разделённых точками:

```
header.payload.signature
```

Пример:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
.
eyJzdWIiOiIxMjMiLCJyb2xlIjoiQURNSU4ifQ
.
abc123signature
```

---

## ✅ 1. Header (заголовок)

Содержит метаданные:

```json
{
  "alg": "HS256",
  "typ": "JWT"
}
```

* `alg` → алгоритм подписи
* `typ` → тип токена

---

## ✅ 2. Payload (данные / claims)

Полезная нагрузка:

```json
{
  "sub": "123",
  "role": "ADMIN",
  "exp": 1700000000
}
```

Типичные claims:

* `sub` → пользователь
* `exp` → срок жизни
* `iat` → когда создан
* `role` / `scope` → права

⚠ Payload **не зашифрован**, только закодирован Base64Url.

---

## ✅ 3. Signature (подпись)

Создаётся из:

```
base64(header) + "." + base64(payload) + secret/privateKey
```

Пример:

```
HMACSHA256(data, secret)
```

Это гарантирует:

✔ токен не подменили
✔ данные не изменяли

---

# 🔐 Как работает JWT в реальной жизни

---

## 🟢 Шаг 1 — Логин

Клиент отправляет логин/пароль:

```
POST /login
```

Сервер:

1. Проверяет пользователя
2. Генерирует JWT
3. Отправляет клиенту

---

## 🟢 Шаг 2 — Клиент хранит токен

Обычно:

* memory (лучше)
* httpOnly cookie (безопаснее)
* localStorage (рискованнее)

---

## 🟢 Шаг 3 — Запросы к API

Клиент прикладывает токен:

```
Authorization: Bearer <jwt>
```

---

## 🟢 Шаг 4 — Сервер проверяет JWT

Сервер:

1. Раскладывает токен
2. Проверяет подпись
3. Проверяет exp / claims
4. Разрешает доступ

Без запроса к БД / сессиям.

---

# 🧠 Почему JWT не требует сессий

В токене уже есть:

✔ кто пользователь
✔ какие роли
✔ срок действия

Сервер **stateless**.


---


# 🔁 Access Token + Refresh Token

Стандартная схема:

* **Access Token** → короткий срок (минуты)
* **Refresh Token** → длинный срок

Когда access истёк → обновляем.

---

# 🆚 JWT vs Session

| Session                 | JWT            |
| ----------------------- | -------------- |
| Состояние на сервере    | Stateless      |
| Легко инвалидировать    | Сложнее        |
| Меньше данных в клиенте | Больше данных  |
| Хорошо для MVC          | Хорошо для API |

---



# Почему TLS 1.3 запрещает renegotiation, и как это влияет на реализацию аутентификации по клиентскому сертификату в REST API?
Отличный вопрос — это как раз про архитектурные изменения в **TLS 1.3** и почему старые ментальные модели из TLS 1.2 больше не работают.

Разберём по слоям.

---

# 🔐 Что такое renegotiation в TLS 1.2

В **TLS 1.2** renegotiation позволяло **переоткрыть handshake внутри уже установленного TLS-соединения**.

Типичный сценарий:

1. Клиент установил HTTPS без клиентского сертификата
2. Сервер принял соединение
3. Клиент сделал запрос к защищённому ресурсу
4. Сервер сказал: *"Окей, теперь покажи сертификат"*
5. Происходил renegotiation → новый handshake → клиент присылает cert

Это позволяло делать:

✅ отложенную клиентскую аутентификацию
✅ условную mTLS
✅ разные уровни доступа в рамках одного соединения

---

# ❌ Почему TLS 1.3 запретил renegotiation

В **TLS 1.3 renegotiation полностью удалён из протокола**.

Причины фундаментальные.

---

## 1️⃣ Безопасность (исторические атаки)

Renegotiation был источником сложных уязвимостей:

* **Renegotiation Injection Attack**
* проблемы связывания старого и нового handshake
* атаки на state machine TLS

Главная проблема:

> Было трудно криптографически доказать, что новый handshake логически продолжает старое соединение.

Это приводило к MITM-векторам и неоднозначностям контекста.

---

## 2️⃣ Упрощение протокола

TLS 1.3 радикально упростил модель:

❌ нет пере-handshake
❌ нет изменения параметров сессии
❌ меньше состояний → меньше багов

Принцип:

> Все параметры безопасности фиксируются при установлении соединения.

---

## 3️⃣ Производительность и latency

TLS 1.3 оптимизирован под:

✅ 1-RTT handshake
✅ меньше round trips
✅ меньше криптографических операций

Renegotiation ломал эти гарантии.

---

# 🧠 Ключевое следствие TLS 1.3

После установки TLS-соединения:

🚫 НЕЛЬЗЯ запросить клиентский сертификат позже
🚫 НЕЛЬЗЯ повысить уровень аутентификации
🚫 НЕЛЬЗЯ изменить cipher / auth параметры

Если нужен client cert → он должен быть частью **первичного handshake**.

---

# 🚀 Как это влияет на REST API с client certificates

Это меняет архитектуру API и безопасность.

---

## ❌ Старый подход (TLS 1.2 mindset)

Раньше можно было:

✔ открыть обычное HTTPS соединение
✔ часть endpoints публичные
✔ часть требует client cert
✔ при доступе → renegotiation

В TLS 1.3 так **не работает**.

---

## ✅ Новый подход (TLS 1.3 правильная модель)

Теперь сервер должен заранее решить:

> Требуется клиентский сертификат или нет?

На уровне TLS listener / port / virtual host.

---

### Вариант 1 — Отдельные endpoints / порты

Типичный production-паттерн:

```
api.example.com        → обычный TLS
mtls-api.example.com   → обязательный client cert
```

или

```
:443  → без mTLS
:8443 → с mTLS
```

---

### Вариант 2 — client-auth = need сразу

Если включить mTLS:

```
client-auth: need
```

то:

✔ сертификат обязателен для ВСЕХ запросов
✔ handshake упадёт без cert
✔ HTTP вообще не начнётся

---

## ⚠ Почему нельзя "условную mTLS"

В TLS 1.3 сервер не может сказать:

> "Этот конкретный HTTP-запрос требует сертификат"

Потому что TLS уже установлен.

TLS не знает ничего про REST, URL, роли.

---







+ JMS. Реализация очередей сообщений. Способы доставки сообщений до клиента. Message-Driven Beans.
+ Java EE CDI Beans стереотипы
+ Angular: модули, компоненты, сервисы и DI.
+ Реализация REST API в Java EE
+ Location Transparency в Java EE
+ JNDI & RMI
+ Как реализовать идемпотентность REST-операций, если клиент может повторно отправить POST-запрос из-за таймаута?
+ Почему Angular не может выполнить change detection для компонента, если он был создан динамически через ViewContainerRef без явного подключения к ChangeDetectorRef?
+ Шаблоны и представление в Angular
+ JAX-RS. Основные аннотации и компоненты
+ Angular: архитектура и основные принципы разработки приложений.
+ Компоненты EJB. Stateless & Stateful Session Beans. EJB Lite и EJB Full.
+ Веб-сервисы. Технологии JAX-RS и JAX-WS.
+ Как клиент должен реагировать на 401 vs 403?
+ Для чего нужен key в React. Что такое Reconciliation и как он работает?
+ Как React Fiber reconciler использует двойное дерево (work-in-progress и current) для атомарного коммита изменений и отката при ошибке?
+ Профили и платформы Java EE
+ Зачем нужны useMemo и useCallback?
+ Как JavaMail API поддерживает отправку multipart-сообщений (например, с вложениями) в enterprise-приложении, и какие MIME-типы используются для структурирования тела письма?
+ Как CSP (Content Security Policy) предотвращает XSS, и почему nonce-based подход безопаснее, чем white-list доменов?
+ Почему Vue не может отслеживать изменения массива по индексу (arr[0] = val) или длине (arr.length = 0) в Vue 2, и как это компенсируется в Vue 3 через Proxy?
+ Как вы решаете какой бин инжектить, если у вас несколько подходящих бинов. Расскажите о @Primary и @Qualifier?
+ JDK dynamic proxy vs CGLib proxy
+ Типы scope-ов
+ Почему передача токена в URL (например, ?token=...) опасна даже при HTTPS?
+ Технология RMI. Использование RMI в Java EE
+ Способы задания конфигурации в Spring
+ Как работает BeanPostProcessor? Spring AOP
+ Почему React не может обновлять компонент, если состояние изменяется мутативно, и как это связано с алгоритмом shallow comparison?
+ Load-time weaving в Spring
+ Во что превращается JSX?
+ Компоненты React. "Умные" и "глупые" компоненты.
+ JSX. Применение в React
+ Платформа Java EE. Спецификации и их реализации
- Что такое Spring MVC?
+ Spring MVC: особенности, интеграция в Spring
+ Разметка страницы в React-приложениях. JSX
+ React. Особенности. Архитектура
+ Что такое Spring Boot? Стартеры
+ Что такое «умные» и «глупые» компоненты в React, и как это способствует разделению ответственности в приложении?
+ Single Page Application(SPA): преимущества, недостатки
+ Что такое island architecture (Astro), и как она снижает объём JavaScript по сравнению с традиционным SPA или SSR-приложением?
+ Как Next.js или Nuxt.js реализуют гибридный рендеринг (SSR + SSG + CSR), и в каких сценариях каждый из режимов даёт максимальную отдачу по Core Web Vitals?
++- Что такое REST?
+ Компоненты React. State & props. "Умные" и "глупые" компоненты
+ Архитектура Flux
+ Основные аннотации JAX-RS
+ Java EE API. Виды компонентов. Профили платформы Java EE
+ Почему для хэширования паролей желательнее использовать алгоритмы вроде bcrypt, а не sha-*?
+ Навигация в React-приложениях. ReactRouter.
+ Платформа Spring. Сходства и отличия с Java EE.
+ Архитектура Angular приложения. Модули, компоненты, представление, сервисы
+ Что такое и как работает Spring BeanPostProcessor?
+ Что такое JWT (JSON Web Token)?
+ Что такое виртуальный DOM в React, и как алгоритм Reconciliation (включая ключи и diffing) минимизирует количество мутаций реального DOM?
+ Реализация REST API в Java EE и Spring.
+ Управление состоянием интерфейса. Redux.
+ Принципы IoC, CDI и Location Transpanency
